apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: test
  namespace: learn
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`test.learn.local`)
      kind: Rule
      services:
        - name: test
          port: 80
